import { EventbusDirectorDirective } from './eventbus-director.directive';

describe('EventbusDirectorDirective', () => {
  it('should create an instance', () => {
    const directive = new EventbusDirectorDirective(null);
    expect(directive).toBeTruthy();
  });
});
